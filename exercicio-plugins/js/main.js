$('document').ready(function(){

    // Mascára para o CPF
    $('#cpf').mask('000.000.000-00')

    // Mascára para o telefone
    $('#telefone').mask('(00) 00000-0000')

    // Mascára para CEP:
    $('#cep').mask('00000-000')
})