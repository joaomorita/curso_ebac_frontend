"use strict";
function calculaArea(base, altura) {
    return base * altura;
}
// shorthand 
const calculaArea2 = (base, altura) => base * altura;
function somar(...numeros) {
    //numeros.reduce()
}
